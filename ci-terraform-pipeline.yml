trigger: none

pool:
  vmImage: "ubuntu-latest"

parameters:
  environment: "dev"
  azureServiceConnection: "ARM_SUBSCRIPTION"
stages:
  - stage: TerraformLintAndFormat
    displayName: "Terraform Lint and format"
    jobs:
      - template: ../jobs/terraform-lint-and-format.yml
        parameters:
          environment: ${{ parameters.environment }}
          azureServiceConnection: ${{ parameters.azureServiceConnection }}
  - stage: TerraformValidate
    displayName: "Terraform validate"
    jobs:
      - template: ../jobs/terraform-validate.yml
        parameters:
          environment: ${{ parameters.environment }}
          azureServiceConnection: ${{ parameters.azureServiceConnection }}
  - stage: TerraformPlan
    displayName: "Terraform plan"
    jobs:
      - template: ../jobs/terraform-plan.yml
        parameters:
          environment: ${{ parameters.environment }}
          azureServiceConnection: ${{ parameters.azureServiceConnection }}
